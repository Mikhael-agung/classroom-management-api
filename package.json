{
  "name": "university-management-api",
  "version": "2.0.0",
  "description": "API untuk sistem manajemen universitas dengan 8 jurusan dan 4 fakultas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node server.js --seed",
    "seed:dev": "nodemon server.js --seed",
    "seed:direct": "node data/seedData.js",
    "test": "jest --testTimeout=10000",
    "test:watch": "jest --watch --testTimeout=10000",
    "clean": "node scripts/clean-database.js",
    "lint": "eslint ."
  },
  "keywords": [
    "university",
    "management",
    "schedule",
    "booking",
    "mongoose",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.0",
    "mongoose": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "@faker-js/faker": "^8.4.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "eslint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}